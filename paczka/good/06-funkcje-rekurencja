int foo() {}

string bar(int a, string d, bool e) {
    return d;
}

int fib(int n) {
    if (n == 0) {
        return 0;
    }
    else if (n == 1) {
        return 1;
    }
    else {
        return fib(n - 1) + fib(n - 2);
    }
}

int main() {
    foo();
    printLnInt(fib(15));
    printLnString(bar(1, "Hello, world!", true));
}
