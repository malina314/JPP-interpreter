Current working directory is `paczka`. Every command should be executed from this directory.

```
make                # build
make test           # run tests
make && make test   # build and run tests
```
